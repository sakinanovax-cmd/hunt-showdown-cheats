# Production site URL — used for canonical URLs, sitemap, RSS, and Open Graph.
# Local dev (default if unset): http://localhost:8080
#
# Production builds use `.env.production` (committed):
# NEXT_PUBLIC_SITE_URL=https://huntcheats.com
#
# Cloudflare dashboard should also set the same variable for Production builds.

